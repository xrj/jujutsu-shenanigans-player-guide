# 生财关卡 4 提交说明

这是 Jujutsu Shenanigans Player Guide 的 GitHub 上传副本，保留关卡 4 静态代码、共享 `assets/`、校验脚本 `scripts/`，并附上最新 AnvilWiki 预览站的全量截图素材。最新截图对应 `http://127.0.0.1:4322/`，不是旧版 `4173` 静态站。

## 本地运行

在仓库根目录执行：

```powershell
py -3 -m http.server 4173
```

浏览器打开 `http://127.0.0.1:4173/` 可运行仓库内的静态代码；最新网站截图来自工作区中的 AnvilWiki 预览 `http://127.0.0.1:4322/`。

## 关卡 4 素材

进入 `关卡4提交素材-JujutsuShenanigans-2026-08-20/`，其中包含：

- `图片/` 下 6 张最新站点关键页面截图；
- `最新站点截图-2026-08-20/` 下 163 个英文、日文、中文页面的 Google Chrome 全页截图；
- `截图记录.json` 和 `路由清单.json`，用于逐页核验截图与路由；
- 可直接粘贴的复盘文字；
- 截图清单、来源与操作记录；
- GitHub 仓库文件列表截图。

本仓库不包含 API Key、Cookie、MCP 配置或个人令牌。
